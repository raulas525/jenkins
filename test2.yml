pipeline {
    agent any
    
    environment {
        NOMBRE = 'raul'
        APELLIDO = 'sandoval'
    }
    
    stages {
        stage ('Build') {
           steps {
               sh 'echo $NOMBRE $APELLIDO'
           }
    }
    }
}
